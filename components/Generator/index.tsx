import { VTitle } from "../VTitle"

export const Generator = ({}) => {
  return (
    <div>
      <VTitle type="h1" className="!text-red-500 text-center">WELCOME TO THE GENERATOR</VTitle>
    </div>
  )
}