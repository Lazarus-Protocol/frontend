import { VTitle } from "../VTitle"

export const PolyVidya = ({}) => {
  return (
    <div>
      <VTitle type="h1" className="!text-red-500 text-center">WELCOME TO POLYVIDYA</VTitle>
    </div>
  )
}